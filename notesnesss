:root {
    --primary: #87CEEB;
    --secondary: #808080;
    --bg: #f9f9f9;
    --white: #fff;
    --green: #28a745;
    --red: #dc3545;
    --dark-primary: #5a9fd4; /* Added for hover states */
}

body.inside-theme {
    --primary: #D2B48C;
    --secondary: #fff;
    --bg: #f0e8d9;
}

body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background: var(--bg);
    color: #333;
    transition: all 0.3s;
}

/* Header Styles */
header {
    background: var(--primary);
    padding: 1rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

#searchBar {
    padding: 0.5rem;
    border: none;
    border-radius: 20px;
    width: 200px;
}

#totals {
    display: flex;
    gap: 1rem;
    font-weight: bold;
}

.total-income { 
    color: var(--green); 
}

.total-expense { 
    color: var(--red); 
}

/* General Button Styles */
button {
    background: var(--secondary);
    color: var(--white);
    border: none;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    cursor: pointer;
}

button:hover { 
    opacity: 0.8; 
}

/* Three Dots and Dropdown */
#threeDots {
    border-radius: 50%;
    width: 40px;
    height: 40px;
    font-size: 1.2rem;
}

#dropdown {
    position: absolute;
    top: 60px;
    right: 1rem;
    background: var(--white);
    border: 1px solid var(--primary);
    border-radius: 5px;
    display: none;
    z-index: 10;
}

#dropdown button {
    display: block;
    width: 100%;
    text-align: left;
    border-radius: 0;
}

/* Main Content */
main {
    padding: 2rem;
    max-width: 1200px;
    margin: 0 auto;
}

#sheets {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1rem;
    margin-bottom: 2rem;
}

.sheet {
    background: var(--white);
    border: 2px solid var(--primary);
    border-radius: 10px;
    padding: 1rem;
    text-align: center;
    cursor: pointer;
    position: relative;
}

.sheet:hover { 
    transform: scale(1.05); 
    transition: 0.2s; 
}

.sheet-icon { 
    font-size: 2rem; 
    color: var(--primary); 
    margin-bottom: 0.5rem; 
}

.sheet.selected { 
    background: var(--primary); 
    color: var(--white); 
}

.sheet-options { 
    display: none; 
    position: absolute; 
    top: 0; 
    right: 0; 
    background: var(--secondary); 
    padding: 0.2rem; 
    border-radius: 0 10px 0 10px; 
}

.sheet:hover .sheet-options { 
    display: block; 
}

.sheet-options button { 
    background: none; 
    color: var(--white); 
    padding: 0.2rem; 
    font-size: 0.8rem; 
}

/* Fixed Buttons (Plus and Order) */
#plusBtn, #orderBtn {
    position: fixed;
    bottom: 20px;
    border-radius: 50%;
    width: 60px;
    height: 60px;
    color: var(--white);
    font-size: 2rem;
    cursor: pointer;
}

#plusBtn { 
    right: 20px; 
    background: var(--primary); 
    display: none; 
}

#orderBtn { 
    left: 50%; 
    transform: translateX(-50%); 
    background: var(--secondary); 
    display: block; 
}

/* Modal Styles - Centered for Add and Settings, Full-Screen for Order */
#addModal, #settingsModal {
    display: none;
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    background: var(--white);
    padding: 2rem;
    border-radius: 10px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.3);
    z-index: 20;
    width: 90%;
    max-width: 500px;
    max-height: 80vh;
    overflow-y: auto;
}

/* EDITED: Full-Screen Order Modal - Portrait Vertical Layout (Stacked Boxes, No Landscape) */
#orderModal {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh; /* Full portrait height */
    background: var(--white);
    padding: 0;
    border-radius: 0;
    box-shadow: none;
    z-index: 20;
    overflow: hidden; /* No body scroll; internal scrolling in menu box */
    display: flex;
    flex-direction: column; /* ADDED: Vertical stacking for portrait (top-to-bottom) */
}

/* Modal Header (Title + Search Bar) - Fixed at Top */
#orderModal > h3 {
    margin: 0;
    padding: 1rem;
    background: var(--primary);
    color: var(--white);
    text-align: center;
    font-size: 1.5rem;
    border-bottom: 1px solid #ddd;
    flex-shrink: 0; /* ADDED: Doesn't shrink in flex */
}

/* Search Bar in Full-Screen Modal */
#orderModal #menuSearchBar {
    width: 100%;
    padding: 1rem;
    margin: 0;
    border: none;
    border-bottom: 1px solid #ddd;
    border-radius: 0;
    box-sizing: border-box;
    font-size: 16px;
    background: #f9f9f9;
    flex-shrink: 0; /* ADDED: Fixed, doesn't shrink */
}

#orderModal #menuSearchBar:focus {
    outline: none;
    background: white;
    border-bottom-color: var(--primary);
}

/* ADDED: Main Content Container for Portrait Stacking (Fills remaining height) */
#orderModalContent {
    flex: 1; /* ADDED: Takes all remaining space after header/search */
    display: flex;
    flex-direction: column; /* ADDED: Vertical stack for portrait */
    overflow: hidden; /* ADDED: No overflow outside boxes */
    width: 100%;
    height: calc(100vh - 140px); /* ADDED: Adjust for header (~60px) + search (~80px); portrait full-height */
}

/* ADDED: Box 1 - Categories (Top Fixed Bar, Small Height for Portrait) */
#categoriesBox {
    background: #f9f9f9;
    border-bottom: 1px solid var(--primary);
    padding: 1rem;
    flex-shrink: 0; /* ADDED: Fixed, doesn't shrink */
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    height: auto; /* ADDED: Auto-height for small top bar (~100px) */
    max-height: 120px; /* ADDED: Limit for portrait */
}

#categoriesBox h4 {
    margin: 0 0 0.5rem 0; /* ADDED: Compact for top bar */
    text-align: center;
    color: var(--primary);
    font-size: 1rem; /* ADDED: Smaller for portrait */
}

/* ADDED: Category Filter Buttons (Horizontal in Top Bar) */
.category-btn {
    background: var(--white);
    border: 1px solid var(--primary);
    padding: 0.5rem;
    border-radius: 5px;
    cursor: pointer;
    transition: all 0.2s;
    font-size: 0.9rem; /* ADDED: Compact for portrait */
    flex: 1; /* ADDED: Equal width in row if wrapped */
}

.category-btn:hover {
    background: var(--primary);
    color: var(--white);
}

.category-btn.active {
    background: var(--primary);
    color: var(--white);
    font-weight: bold;
}

/* EDITED: Box 2 - Menu List (Middle Scrollable Area, Main Portrait Content) */
#orderModal #menuItemsBox { /* EDITED: Changed from #menuList to #menuItemsBox */
    flex: 1; /* ADDED: Grows to fill middle space (~70% height) */
    overflow-y: auto; /* ADDED: Vertical scroll only for menu (portrait focus) */
    overflow-x: hidden; /* ADDED: No horizontal scroll */
    padding: 1rem;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); /* Kept: Responsive grid */
    gap: 1rem;
    margin: 0;
    background: var(--white); /* ADDED: Clean background for scrollable area */
    border: none; /* ADDED: No border for seamless scroll */
}

#orderModal .category {
    margin-bottom: 1.5rem;
}

#orderModal .category h4 {
    text-align: center;
    margin-bottom: 1rem;
    color: var(--primary);
    font-size: 1.2rem;
}

#orderModal .menu-item {
    background: #f9f9f9;
    border: 1px solid var(--primary);
    border-radius: 10px;
    padding: 1rem;
    text-align: center;
    transition: all 0.2s ease;
}

#orderModal .menu-item:hover {
    transform: scale(1.05);
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
}

#orderModal .menu-item i {
    font-size: 2rem;
    color: var(--primary);
    margin-bottom: 0.5rem;
}

#orderModal .menu-item h5 {
    margin: 0.5rem 0;
    font-size: 1rem;
}

#orderModal .menu-item p {
    margin: 0.25rem 0;
    font-weight: bold;
    color: var(--green);
}

#orderModal .menu-item button {
    background: var(--primary);
    color: var(--white);
    border: none;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    cursor: pointer;
    font-size: 0.9rem;
    margin-top: 0.5rem;
}

#orderModal .menu-item button:hover {
    opacity: 0.8;
}

/* EDITED: No Results in Menu Box (Portrait Centered) */
#orderModal #noResults { /* EDITED: Scoped to orderModal */
    grid-column: 1 / -1;
    text-align: center;
    padding: 2rem;
    color: #666;
    font-style: italic;
    background: #f8f9fa;
    border: 1px dashed #ccc;
    border-radius: 5px;
    margin: 1rem; /* ADDED: Spacing in scrollable box */
}

/* EDITED: Box 3 - Calculator/Summary (Bottom Fixed Area) */
#